// Função normal...
function soma(number1, number2){ 
    return number1 + number2;
};

soma(2, 2);
soma(2, 3);
soma(2, 4);
soma(2, 5);

// Perceba que o valor 2 sempre está sendo passado como parâmetro